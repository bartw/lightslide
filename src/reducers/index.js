const initialState = {
    slides: [],
    url: '',
    currentIndex: 0,
    showSlideShow: false
};

const lightSlideApp = function (state = initialState, action) {
    switch (action.type) {
        default:
            return state;
    }
}

export default lightSlideApp;